[user]
	name = Luc Fouin
	email = luc.fouin@gmail.com
[credential]
	helper = cache --timeout=3600
[alias]
    st = status
    df = diff
    ci = commit
    co = checkout
    br = branch
    lg = log --graph --date-order --date=short --pretty=format:'%C(auto)%h%d %C(reset)%s %C(bold blue)%ce %C(reset)%C(green)%cr (%cd)%C(reset)'
[push]
	default = matching
