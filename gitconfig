[user]
	name = FOUIN Luc (i-BP)
	email = luc.fouin@i-bp.fr
[core]
    pager = cat
    editor = vim
[push]
	default = simple
	followTags = true
[branch]
	autosetuprebase = always
[alias]
    ph = push
    pl = pull
    st = status
    d = diff
    df = diff
    ci = commit
    co = checkout
    br = branch
    lg = !git --no-pager log -n 10 --graph --date-order --date=short --pretty=format:'%C(auto)%h%d %C(reset)%s %C(bold blue)%ce %C(reset)%C(green)%cr (%ci)%C(reset)'
    find-merge = !"f() { git rev-list $1..master --ancestry-path --merges --reverse | head -1; }; f"
[http]
	sslVerify = false
	proxy = http://proxy.ctr.ibp:8080
[https]
	proxy = http://proxy.ctr.ibp:8080
[safe]
	directory = %(prefix)///wsl$/Ubuntu-22.04/home/inefoul/w
